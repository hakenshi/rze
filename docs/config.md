# Config (Implementer Notes)

Config is TOML: `$XDG_CONFIG_HOME/rze/config.toml`.

Initial v0 config should be minimal:

- wallpaper backend selection and overrides
- reset enable/disable
- template dir override (optional)
