# Diagrams (Implementer Notes)

## Layers

cli -> app -> core
       ^
       |
     infra

## rze img pipeline

img -> resolve input -> persist wallpaper -> set wallpaper -> palette -> state -> render -> deploy -> reset
